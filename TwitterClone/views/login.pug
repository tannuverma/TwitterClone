extends layouts/login-layout.pug 

block content 
    .loginContainer 
        h1 Login to Twitter
        form(method = "post")

            p.errorMessage #{errorMessage}
            //- input(type="text", name="logUsername", placeholder="Username or Email", value=logUsername required="")
            //- input(type="password", name="logPassword", placeholder="Password", required="")
            input(type="text", name="logUsername", placeholder="Username or Email", value="tannuverma" required="")
            input(type="password", name="logPassword", placeholder="Password",value="tannu123", required="")
            input(type="submit", value="Login")
        a(href="/register") New User? Register here.
